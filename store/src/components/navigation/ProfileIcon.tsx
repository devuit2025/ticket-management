import React from 'react';
import { Ionicons } from '@expo/vector-icons';

const ProfileIcon = () => (
    <Ionicons name="person-circle-outline" size={28} color="#007AFF" style={{ marginRight: 15 }} />
);

export default ProfileIcon;
